# コマ（仮）— 全体像（v1・2026-09-17）

> 1日を「時間帯の枡（ます）」で切って、やること・食事・運動…を同じ骨組みで記録する iPhone アプリ。
> ライフログ（`owntracks-supabase-notion`）の ◻️ やること と 🍽 食事 を持ち込み、種目を増やせる形に一般化したもの。
> 名前「コマ」は仮（＝時間割のコマ・升目のこと）。リポ名 `dayslots` も仮。

## 1. 何を作るか（1段落）

週の画面は **縦＝日・横＝時間帯の枡**（やること＝時間帯なし／午前／昼／午後／夕方以降、食事＝朝食／昼食／夕食／間食）。1件の記録には **予定の時刻**と**実際の時刻**を別々に持て、決まっているときはそれも見える。枡の境目（11:00 / 14:00 / 17:00 …）は変えられ、枡の数も増やせる・減らせる。「やること」「食事」に加えて「運動（朝散歩／夜ジム）」のような**種目**を足せる。⭐ いつもの（型）と 🔁 繰り返し（規則）はライフログの作法を写す。時刻が決まっている記録は **Google カレンダーに出せる**。

## 2. ライフログから何を持ち込むか（棚卸し）

| 機能 | ライフログでの形 | この app での形 | 段階 |
| --- | --- | --- | --- |
| 時間帯の列（やること） | `ST_TOD` 境目 11/14/17 ＋ `slot` 列（人が選ぶ）＋「時間帯なし」 | 種目「やること」の枡。境目は種目ごとに変えられる | ✅ v1 |
| 食事の枠 | `MEAL_SLOT_DEF` 朝 4:00–10:30／昼 10:30–15:00／夕 16:30–／間食＝受け皿 | 種目「食事」の枡。隙間は受け皿（間食）へ | ✅ v1 |
| 人が選んだ枡は自動で動かさない | `slot_by='human'`／`tdBucketOf` | `slotKey` が勝つ。無ければ時刻から | ✅ v1 |
| 境目は保存しない＝読むときに導く | localStorage の境目・記録には焼かない | 同じ。境目を変えると過去も並び直る | ✅ v1 |
| ✅ 済／🚫 未実行／🔀 代わりに | `done_at`／`skipped_at`（両立しない CHECK）／`instead_of_id` | 同じ列・同じ規則 | ✅ v1 |
| 予定の時刻と実際の時刻 | `do_min`／`done_date`・`done_min` | `planStart/End`／`actualDate`・`actualStart/End` | ✅ v1 |
| 週の見直しの帯（✅ 51 🚫 9 まだ 32 🔁 4） | 週グリッドの上の緑の帯 | 同じ位置に同じ数字 | ✅ v1 |
| ⭐ いつもの（型） | `meal_presets`：型は事実ではない・使った回数は導出・枡は既定で縛りでない | `Template`：同じ約束。全種目で使える | ✅ v1 |
| 🔁 繰り返し | `todo_recurring`＋`recurOccurrences`：自動／確認、未来に作らない、(recur_id, recur_date) unique、例外 | `Rule`＋`occurrences()`（規則をそのまま写した）。全種目で使える | ✅ v1 |
| 休日＝土日＋祝日＋有給 | `isHoliday`・`custom_holidays` | 同じ。⚙ で休みを足す | ✅ v1 |
| 重要度・🏷 集計ラベル | `priority`・`tag` | 同じ列 | ✅ v1（tag は器だけ） |
| 📷 食事の写真・AI 書き起こし | `meals.path/thumb`・Edge Function `meals`（AI・`ai_settings` に鍵） | 写真と AI 書き起こしは **Phase 2**（ゆう 2026-09-18「いれてください」）。要約／詳細／料理の行／出どころ を書き起こし、**数値（カロリー等）は作らせない**。鍵はサーバ側（Edge Function `koma-ai`）＝ライフログと同じ | ⏳ Phase 2 |
| 📋 一覧（期限だけ決まったもの） | `due_date`・「まだ」の一覧 | 期限の列は今は持たない（要るとき足す） | ⏳ |
| 場所・地図 | `lat/lon/place_name` | 持たない（ライフログの仕事） | ✗ |
| 📅 Google カレンダー | Edge Function `gcal`（双方向・地平線・map） | 片方向（出す）から。Edge Function 方式は同じ | ⏳ Phase 4 |

## 3. 骨組み＝3つの言葉＋2つ

```
種目 Track ──┬── 枡 Slot ×N   （時間帯の並び。境目・数・呼び名は種目ごと）
             ├── 記録 Entry ×N（1行＝その日の1件。予定の時刻／実際の時刻／✅🚫🔀）
             ├── ⭐ いつもの Template ×N（記録の「型」。呼んでから記録を作る）
             └── 🔁 繰り返し Rule ×N（規則。回 Occurrence に展開 → 記録にする）
```

- **種目（Track）**＝記録の種類。やること／食事／運動…。枡の並びと「使うもの」（✅ を使うか、📷 を使うか、📅 に出せるか、実際を主にするか）を持つ。
- **枡（Slot）**＝1日を切った時間帯。2種類ある：**時刻で決まる枡**（始まりの時刻を持つ。午前・朝食…）と、**時刻で決まらない枡**（始まりが空。「時間帯なし」「間食」＝人が選んだときだけ入る）。
- **記録（Entry）**＝その日のその1件。`date`（やる日／食べた日）・`slotKey`（人が選んだ枡・空なら時刻から）・予定の時刻・実際の時刻・✅／🚫／🔀・⭐ の元・🔁 の元。
- **⭐ いつもの（Template）**＝記録の型。**型は事実ではない**ので升目にも数にも出ない。使った回数・最後に使った日は記録から数える（保存しない）。
- **🔁 繰り返し（Rule）**＝「毎日」「毎週 月水金」「週の最初の平日」「月の後半」など14種。`auto`（自動で記録になる／薄く出して押した日だけ）と例外（この回はなし）。

コードの場所: `src/domain/types.ts`（言葉）／`slots.ts`（枡の決まり）／`recur.ts`（🔁 の展開）／`src/app/repo.ts`（操作の台帳）。

## 4. 枡（時間帯）の決まり

| 決まり | 中身 | なぜ |
| --- | --- | --- |
| 境目は保存しない | 記録には時刻と「人が選んだ枡」だけ。どの列に出すかは**読むとき**に決める | 境目を変えたら過去も一緒に並び直る（ライフログの判断と同じ） |
| 人が選んだ枡が勝つ | `slotKey` があればそれ。無ければ主な時刻から。時刻も無ければ受け皿 | 「13:50 に始めたが午後にやったと言える」のは本人だけ |
| 主な時刻 | 食事のように「実際を主にする」種目は実際の時刻（無ければ予定）。やることは予定（無ければ実際） | 食事は食べた事実が主・やることはやる前に置く |
| 隙間は受け皿 | 時刻で決まる枡が覆わない時刻（食事の 15:00〜16:30・深夜）は受け皿の枡（間食）へ | ライフログの食事と同じ受け止め |
| 消えた枡の値は書き換えない | 種目から枡を消しても、記録の `slotKey` はそのまま。読むときに 時刻→受け皿 に落とす | 記録が画面から消えない・値の意味が黙って変わらない |
| 増やす・減らす・並べ替え | ⚙ → 種目 → 枡を直す。足す／消す／始まり／終わり／↑↓／受け皿 | 検査（`validateSlots`）を通らないと保存できない |
| 見出しの範囲 | 「11:00〜14:00」は境目から**作る**（手で書かない） | 境目を動かしても説明だけ古く残らない |

## 5. 記録の状態

- **予定の時刻**と**実際の時刻**は別の列。1日の画面では並べて見える（予定 15:20–15:45 ／ 実際 —）。
- ✅ やった（`doneAt`）と 🚫 やらなかった（`skippedAt`）は**1行で両立しない**。片方を立てると他方が消える。戻すときは他方に触らない。
- 🔀 代わりに＝元は 🚫 で閉じ、代わりの記録が `insteadOfId` で元を指す。元の側に印は持たない（「代わりで閉じた」は導出）。
- ✅ を立てたとき実際の日が空なら「その日」を入れる。時刻は入れない（分からないものを作らない）。

## 6. ⭐ いつもの と 🔁 繰り返し（ライフログから写した約束）

- ⭐：型の枡は**既定であって縛りではない**（朝食の型を間食にも使える＝呼んだ先の枡が勝つ）。写真は記録と**共有**（実体を増やさない）。型を消しても記録は残る。
- 🔁：`auto=true`＝その日が来たら記録が入る（毎朝の青汁）。`auto=false`＝升目に薄く出て、押した日だけ記録になる（やらなかった日の未完了が溜まらない）。**未来の日には作らない**。同じ回は `(ruleId, ruleDate)` で1行。例外＝この回はなし（記録も消える＝🚫 とは別物：🚫 は「やらなかった」を薄く残す）。
- 休日の判定は `isHoliday` ただ1つ（土日＋日本の祝日＋⚙ で足した休み）。「休日の前日」は金曜ではなく**翌日が休日の平日**。

## 7. 📅 Google カレンダー

- **何を出すか**＝本人が「出す」を立てた記録。時刻があれば**その時刻**で、時刻が無い（枡だけ・「時間帯なし」）なら**終日**で出す（ゆう 2026-09-18「だしてください」）＝嘘の時刻は作らず、日と枡の名前（説明欄）は伝える。✅ 済で実際の時刻があれば実際で出す。
- **どう出すか**＝ライフログの `gcal` と同じ **Edge Function 方式**（Google の refresh token はサーバの Secret に置き、画面は合図だけ）。関数名 `koma-gcal`。呼ぶ形は `src/sync/calendar.ts` の `GoogleViaEdgeFunction` に先に決めてある（`{op:'upsert'|'remove', event, eventId}`）。
- 記録 ⇔ Google の予定の対応は `calendarMap`（中身の指紋つき＝同じなら書かない）。出さなくなった記録は Google 側を消す。
- 代案（検討のみ）＝端末で Google にログインする OAuth。利用者が本人だけなので、いまは Edge Function 方式が手間が少ない（ライフログで動いている・7日失効の罠の回避も済んでいる）。
- 双方向（Google → app）は**やらない**（ライフログがやっている＝二重にやるとぶつかる）。

## 8. 技術の選び方

| 何 | 選んだもの | なぜ／代案 |
| --- | --- | --- |
| iOS の作り方 | **Capacitor 8**（web を iOS の殻で包む）＋ **Codemagic**（Mac 無しで焼く） | 既に「あの日」「声で入れるカレンダー」で動いている作法（署名・TestFlight の手順が手元にある）。Windows で開発できる。代案＝Expo（React Native）：見た目は寄せやすいが新しい道具が丸ごと増える／Swift：Windows では画面を確かめられない |
| 画面 | **TypeScript ＋ Vite**、枠組み無し（`h()` で DOM を作る） | ライフログの index.html と同じ「関数で描く」考えのまま、ファイルを分けて型を付けた。React 等を入れると覚える言葉が増える |
| 置き場（データ） | v1＝**端末（localStorage）**→ Phase 3＝**Supabase**（ライフログと同じプロジェクトに `koma_` の表） | まず端末で形を確かめる。Supabase に移すのは `Store` の口の後ろだけ（画面と規則は触らない）。表の形は `supabase/migrations/0001_init.sql` に先に書いてある（⚠ 未適用） |
| ライフログとの関係 | **別 app・別の表**。ライフログの `todos`/`meals` からの取り込みは Phase 3 の宿題 | 食事の `slot` が CHECK で4つに縛られている等、一般化した枡と器が合わない。表示だけライフログに出す（📊 に数える）のは後で決める |
| 検査 | Vitest（枡の決まり・🔁 の展開・台帳の規則） | 負のテスト（わざと壊す）を含める＝ゆうの流儀 |

## 9. 画面

1. **週**（既定）＝縦7日 × 横＝種目の枡。升目の中に記録のチップ（✅／◻️／🚫・時刻・名前・🔁⭐📅🏠 の印）。🔁 の薄い回（押すと記録・✕ でなし）。升目の「＋」でその日その枡に足す。上に **週の見直しの帯**（✅ n 🚫 n まだ n 🔁 n／食事は件数と 🏠🏪🍴）。
2. **1日**＝枡ごとの一覧。1行に「予定 HH:MM–HH:MM ／ 実際 HH:MM–HH:MM」と名前。
3. **記録の板**＝なに／枡（時刻から自動 or 選ぶ・いまの置き場を表示）／予定／実際／状態（✅🚫🔀・重要度）／出どころ（食事）／メモ／📅／⭐ から選ぶ・⭐ に登録／🔁 にする／保存・消す。
4. **⭐ いつもの**＝一覧（使った回数・最後の日）→ この日に入れる／直す／消す／手で作る。
5. **🔁 繰り返し**＝一覧（規則の説明・自動／確認・なしの回の数）→ 止める／直す／消す／足す。板では14種の「いつ」・曜日・N日・前半後半・N〜M日・始まり終わり・枡・時刻・入り方・📅。
6. **⚙ 設定**＝種目（枡を直す・畳む・足す）／週の始まり／📅 の接続／休み／データ（JSON 書き出し・読み込み・見本に戻す）。

## 10. 段階

| 段階 | 中身 | 状態 |
| --- | --- | --- |
| 0 | リポ・骨組み・言葉・枡と 🔁 の規則・端末で動く画面・検査 | ✅ この回（v1） |
| 1 | iPhone で触る：Capacitor iOS の殻を足す → Codemagic → TestFlight。実機で枡の幅・板の押しやすさを直す | ▶ 次 |
| 2 | 📷 写真（Capacitor Camera・縮小して保存）。食事の升目に写真を出す。**AI 書き起こし**（写真 → 要約・詳細・料理・出どころ。Edge Function `koma-ai`・鍵はサーバ） | |
| 3 | Supabase の置き場（`koma_` の表＋Edge Function `koma`）。複数端末で同じ記録。ライフログ `todos`/`meals` の取り込み（一度だけ） | |
| 4 | 📅 Edge Function `koma-gcal`（Google に出す・消す）。設定画面の「いま送る」が実際に動く | |
| 5 | 発展＝📊 数える（枡別・種目別の週次）、期限だけ決まったやることの一覧、ライフログ 📊 への表示 | 本人と決める |

## 11. まだ決めていないこと（本人に聞く）

1. **名前**（コマ／dayslots は仮）と bundle id（`io.github.yutsutke.koma` は仮）。
2. **ライフログの記録を取り込むか**（一度だけ写す／写さず新しく始める／両方に書く）。Phase 3 の入口で決める。
3. **Supabase は同じプロジェクトか別か**。同じなら 📊 で一緒に数えやすい。別なら壊す心配が無い。

**決まったこと（2026-09-18・ゆう）**: 食事の AI 書き起こしは**入れる**（Phase 2）／「時間帯なし」の記録は Google に**終日で出す**（v2 で実装）／リポは **public**（GitHub Pages で開発のページを出すため＝private では Pages が使えない）。

## 12. 開発のページ

main に push すると GitHub Actions が 検査 → 焼く → **https://yutsutke.github.io/dayslots/** に出す（`.github/workflows/pages.yml`）。あの日・声で入れるカレンダーと同じく、iPhone の Safari で開いて触れる（記録はその端末の中）。
