# TODO — 現在地と次の一手

## 現在地（2026-09-23・v31）

- ✅ v1〜v16 を2日で。動くもの＝週/1日/月・⊞ すべて・一日一回（座禅）・⏱ 長さだけの入力と合計/平均・表示の切替 🕐⏱💬📷・合図（「座禅開始／散歩終了」・🎤・進行中の常時1行・自動終了・上限の確認）・🧾 レシート（📷 → 🤖 BYOK）・種目の並べ替え・☁ 外の写し（Supabase＝**本番で動いている**／Google ドライブ＝未検証）・送る期間・差分・初回は聞く・📝 振り返りの要約（`koma_docs.review`）。
- ✅ v17（2026-09-20）＝📋 リスト／☀ 日の出・日の入りで切る枡（端末で計算・5分刻み・場所は ⚙）。
- ✅ v18（2026-09-20）＝▶ スタート／⏹ ストップ・なに未定の計測／リストの基準日／🌙 夜を等分（未明は前の晩の枡の続き）。
- ✅ v19（2026-09-20）＝1日の始まりを 0:00／日の出／日の入り から選ぶ（見せる日だけ変わる・記録は書き換えない）。
- ✅ v20（2026-09-20）＝升目の列も 1日の始まりの枡から並ぶ（日の入り始まり＝夜 → … → 午後）。
- ✅ v21（2026-09-20）＝**ズレを読む**（予定と実際の差＝時刻／長さ／日）。記録には1列も足さず、読むときに引く（`src/domain/gap.ts`）。升目・1日の行・見直しの帯・📝 振り返りに出る（画面のぶんは ⚖️ で出す・隠す）。憲法に8条「大きいものは記録に埋めない」を追加。
- ✅ v22（2026-09-20）＝**⏭ 先送り**（✅🚫🔀 とは別の第3の道）。`date` を後ろへ動かし、もともとの日と回数を履歴に残す（`src/domain/postpone.ts`）。先送りの後でも ✅／🚫／🔀 を選べる。
- ✅ v23（2026-09-20）＝**N日のひと区切り（サイクル）**。週（曜日で切る暦の区切り）とは別に、3日・10日 などの幅で見る。切るのは読むときだけ（`src/domain/cycle.ts`）。⚙ で日数と数え始める日。
- ✅ v24（2026-09-20）＝**📷 手の形で合図**（☝＝開始／✌＝終了…は ⚙ で自由に）。決まった顔ぶれから AI が選ぶだけ＝言葉を作らせない。写真は残さない（`src/domain/gesture.ts`）。✅ **本物の写真で読めた**（2026-09-21・本人の鍵・実機）。
- ✅ v25（2026-09-20）＝**AI の呼び先を作り直した**（声で入れるカレンダー `engine/ai.js` を写した）。**OpenRouter を追加**（1つの鍵で GPT・Claude・Gemini）／モデルの一覧を先方に聞く／鍵をエラー文に出さない／呼び先の理由を捨てない／時間切れを名指す／Gemini の鍵を URL からヘッダへ。✅ **実機で OpenRouter を呼べた**（2026-09-21・本人の鍵）＝一覧・生成・画像の包み方（`image_url`）まで本物で通った。
- ✅ v26（2026-09-21）＝**頭を畳めるように**（実機の感想から）。並びを 種目 → **合図・録音** → 見方 に変え、種目と合図を畳めるようにした（端末ごとに記憶・畳んでも種目は選べる）。見直しの帯がスマホで1文字ずつ割れていたのも直した。
- ✅ v27（2026-09-21）＝**📆 長い期間（3か月・半年・1年）**。たまにしかやらないこと用。縦7（曜日）× 横（週）の濃淡のマス目＋「最後にやった日・だいたい何日に1回・いちばん空いた間隔」（`src/domain/span.ts`）。この画面では 🔁 自動の回を作らない（見るだけの操作を書き込みにしない）。
- ✅ v28（2026-09-21）＝**⚙ を4つの層で章立て**（① 入れる ② 残す ③ 読む ④ 出す・上に飛び先の帯）。ついでに `.field` の既存の不具合を直した＝スマホの縦並びで `flex-basis:96px` が**高さ**として効き、見出しと入力が 96px 離れていた（⚙ と記録の板の全部の欄）。
- ✅ v29（2026-09-23）＝**📷 サムネを記録の外（IndexedDB）へ**（憲法8条の最初の宿題）。新しい写真は本体もサムネも棚に置き、記録には `idb:<id>` の指だけ。古い記録のサムネは開いたときに移す（置けたものだけ外す＝消えない）。別の端末で撮った写真は灰色の 📷 で出る（`src/domain/thumbs.ts`）。
- ✅ v30（2026-09-23）＝**🗓 記念日（ライフログから）**。正本はライフログの表 `milestones`・`milestone_logs`＝コマは関数 `koma-milestones`（本番に置いた）で読み・足し・直す。升目の日の見出しに 🗓 節目（±1/3/6ヶ月・N年前）と 🔔 リマインダー、🗓 ボタンで一覧（N日目／あとN日・次の周年）と記録ログ。決まりは `src/domain/milestones.ts`（ライフログと同じ）。
- ✅ v31（2026-09-23）＝**🗓 記念日ごとにタブ**（1つの種目のように見る）。中身はライフログの記録ログ（週・N日・1日＝1日1行／月＝升目／リスト・長い期間＝並び）、＋ で足す・押して直す。**タブを「ふだん見せる／畳んでおく」に分けられる**（⚙ → ② 残す の「タブ」・畳んだものは「ほか ▾」）。✅ v30 は実機で記念日が出た（2026-09-23）。
- 開発のページ https://yutsutke.github.io/dayslots/ （main に push → Actions）。Supabase はライフログと同じプロジェクト（表 `koma_docs`・関数 `koma-store` v6・Secret `KOMA_SECRET`）。
- ✅ **📝 振り返りの要約を実機で確かめた**（2026-09-21・本人が Supabase で読めた＝AI に渡す道が通った）。読む場所は3つ＝**⚙ → ④ 出す の「N日ぶんを見る」**（その場で作り直す・外に送っていなくても、鍵も SQL も要らない）／**Supabase** `select review->>'text' from koma_docs where id='default'`（振り返りのとき AI にそのまま読ませられる＝便利）／**Drive** `koma-review.md`。v21 の「予定とのズレ」・v22 の ⏭ 先送りもこの読み物に入っている。
- ▶ **次の回の最初に**＝別の端末（iPhone）でつないで「外の写しを取り込む」を試す（v15 の初回の聞き方が実機で分かりやすいか）。
- ▶ **v31 を実機で**＝記念日のタブで記録ログを1件足して、ライフログ側に出るか（読むのは v30 で実機が通った・書くのはまだ）。スマホで10個の記念日タブが並んだときに、どれを畳むか。
- ▶ **v29 を実機で**＝写真のある記録を開いて、サムネがそのまま見えるか・⚙ の保存量が減ったか（写しも細る）。
- ▶ 本人に聞く未決（SPEC §11）＝①アプリの名前 ②ライフログの記録を取り込むか。調査の「次の一手」16項目は docs/reports/コマ 似たアプリの調査.md（1位＝URL `?say=` で合図を受ける口）。

### 現在地にぶら下がっている宿題
- [ ] 🗓 周年そのもの（同じ月日の「（12年）」）を升目に出すか＝v30 では選ばれなかったので出していない（一覧には「次の N年まで」がある）。要るなら `marksOn` に1種足す
- [ ] 🗓 ライフログの `msCalDiff` は 1/31 → 3/1 を「1ヶ月-2日」にする＝ライフログ側の不具合（コマでは写さず直した）。ライフログで直すか本人に聞く
- [x] 🤖 **OpenRouter で実際に1回呼んでみる** ＝ 2026-09-21 に実機で通った（一覧・生成・画像の包み方 `image_url`）。**v25 の作りはこれで裏が取れた**
- [x] 📷 手の形＝**本人の鍵で1枚読ませる** ＝ 2026-09-21 に実機で通った。顔ぶれ6つのまま進む（読み違いが目立つようなら ☝✌ の2つに減らす＝`GESTURES` の1行）
- [ ] 📷 **カメラで撮ってすぐ**（いまは写真をえらぶだけ）＝Phase 1〜2（@capacitor/camera）
- [ ] 先送りの**取り消し**（1回戻す）は作っていない＝間違えたら記録の板で日付を直す（履歴はそのまま残る）。要るようなら足す
- [ ] 先送りを**合図**からも（「伝票 来週に先送り」）＝`src/domain/signal.ts` に動詞を足すか決める
- [ ] **やることの親子構造**（`parentId`）＝まだ無い。作る前に「親を 🔁 繰り返したら子はどうなるか」を先に決める（軸と軸が交わる所）
- [ ] 🧾 品目の手直し（読み違いを直す欄）／🧾 と 🍽 の結び付け（同じ食事のレシート）
- [ ] 📷 写真の本体（IndexedDB）は端末の外に出ない＝JSON 書き出しに入らない。Phase 3 で Supabase Storage へ
- [ ] 合図の受け口を外に開ける（URL `?say=座禅開始` → Siri のショートカット／ウィジェットから）＝Phase 1 と一緒に
- [ ] 日をまたいだ終了（23:50 開始 → 0:10 終了）は今は終わりを 24:00 に丸めてメモに残す＝2日にまたがる記録の持ち方を決める
- [ ] ☁ Google ドライブ：本人が Client ID を作って実際に読み書きを確かめる。iOS の殻でログイン窓が開かなければ ネイティブのログイン（@codetrix-studio/capacitor-google-auth 等）に替える
- [ ] ☁ 2台で同時に書いたときの取りこぼし（丸ごと1行＝あとに保存した方が勝つ）＝行単位の同期は Phase 3
- [ ] ☁ 写真の本体（IndexedDB）は写しに入らない＝Drive/Supabase Storage へ別に置く（Phase 2〜3）。**v29 からはサムネも入らない**＝別の端末では灰色の 📷 になる（撮った端末にはある）

## Phase 1 — iPhone で触る
- [ ] `npx cap add ios`（Windows でも殻は作れる。`cap sync` は Codemagic 上で）
- [ ] Codemagic: App Store Connect の API キーを `KomaASC` の名前で登録・`signing` グループに `CERTIFICATE_PRIVATE_KEY`（あの日と同じ手順）
- [ ] bundle id を決める（仮 `io.github.yutsutke.koma`）・App Store Connect に app を作る
- [ ] TestFlight で触って直す：升目の横幅（5枡は横スクロール）・板の押しやすさ・時刻入力の使い勝手
- [ ] 記録の板の「なに」に音声入力（iOS のキーボードのマイクで足りるか確かめる）

## Phase 2 — 📷 写真（食事）
- [ ] Capacitor Camera で撮る／写真から選ぶ。長辺 1568 ＋ サムネ 320 に縮小（ライフログと同じ）
- [ ] 升目のチップに写真を出す（週は サムネだけ）
- [ ] 写真の置き場（v1 は端末・Phase 3 で Supabase Storage）。⭐ と共有＝消すときは両方を数える
- [ ] **AI 書き起こし**（決定 2026-09-18）: Edge Function `koma-ai`（ライフログ `meals` 関数の AI の節を写す・鍵は `ai_settings`）。写真 → 要約／詳細／料理の行／出どころ＋理由。status（todo/pending/done/error）を記録に持つ。**数値は作らせない**。人が直した後は読ませ直す前に確かめる（`edited_at`）

## Phase 3 — Supabase の置き場
- [ ] `supabase/migrations/0001_init.sql` を本人が当てる（同じプロジェクトか別か＝先に決める）
- [ ] Edge Function `koma`（合言葉・行単位の読み書き）。`SupabaseStore` を丸ごとでなく行単位に
- [ ] ライフログ `todos`/`meals` の取り込み（一度だけ写す）＝枡の対応表: todos.slot morning→morning, midday→midday, noon→afternoon, evening→evening／meals.slot はそのまま
- [ ] 複数端末の衝突（updated_at の新しい方が勝つ、で足りるか）

## Phase 4 — 📅 Google カレンダー
- [ ] Edge Function `koma-gcal`（`{op:'upsert'|'remove'}`）。ライフログ `gcal` の token 取り回しを写す（7日失効の罠＝本番昇格）。`allDay:true` は Google の終日（date のみ）で作る
- [ ] 設定の「いま送る」→ 記録を保存したら自動で送る（未送信の数を帯に出すのは v1 で済み）
- [ ] 出したものの色（Google のカレンダーを分けるか）

## Phase 5 — 発展（本人と決める）
- [ ] 📊 数える：枡別・種目別の週次（✅ の率、🏠🏪🍴 の比）
- [ ] 期限だけ決まったやること（`due_date`）の 📋 一覧
- [ ] 食事の AI 書き起こし（要るなら）
- [ ] 帯（月の後半など複数日の回）を升目に帯として出す（v1 は開始日に「〜MM-DD」）
- [ ] 記録の並べ替え（升目の中の順・sortOrder は器だけ）

## 今は作らない
- ライフログとの双方向同期（ライフログ側が Google と双方向＝ぶつかる）
- 場所・地図（ライフログの仕事）
- 数値のカロリー等
