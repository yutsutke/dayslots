# CHANGELOG

## v12.1 — Supabase の写しが実際に動いた（2026-09-18）

- ゆうが本番プロジェクト（ライフログと同じ）に表 `koma_docs`・Secret・関数 `koma-store` を置いて試した → 「Failed to fetch」。原因＝関数が OPTIONS（ブラウザの事前確認）に**本文つきの 204** を返して 500 になっていた（Deno は 204 に本文を許さない）。本文なしに直して置き直し → 「端末の内容を外へ保存しました」（56 件・36KB）。
- **外の写しに 🤖 の鍵と保存場所の設定を入れない**ようにした（取り込むときは端末のものを戻す）＝外に秘密を置かない。
- SPEC §11 ③（同じプロジェクトか別か）＝**試しは同じプロジェクト**で進めることになった（表名 `koma_` でぶつからない）。

## v12 — 種目の並べ替え／保存場所（端末・Google ドライブ・Supabase）／SQLite 書き出し（2026-09-18）

**背景**
- ゆう「トップページで並び替え。設定でも並び替え」「保存場所＝Google ドキュメントを指定して保存（JSON と SQLite）。Supabase も指定できるように」。

**設計判断**
- **並べ替え**＝⚙ の一覧に ↑↓、トップは選んでいる種目のタブの横に ‹ ›（動かすのは選んでいるものだけ＝誤タップで並びが崩れない）。畳んだ種目は ⚙ からだけ 🗑 で消せる（記録の数を言ってから）。
- **保存場所＝「外の写し」**＝端末の保存箱はいつも使い、その丸ごと1つの JSON を外に置く。開いたときに外が新しければ取り込み、保存のたびに3秒後に押し出す。**新しい方（savedAt）が勝つ**。行単位の同期は Phase 3 の宿題（1人1台ずつの前提）。
- **Google ドライブ**＝Google Identity Services（ブラウザで Google のログイン窓を開く方式）・権限 drive.file（このアプリが作ったファイルだけ）・本人が指定した**フォルダ**に koma.json。「Google ドキュメント」は文書の形式なので JSON/SQLite を入れられず、フォルダ指定にした。Client ID は本人が Google Cloud で作る（手順は ⚙ の説明）。⚠ iOS の殻の中ではこのログイン窓が開かないことがある＝TODO。
- **Supabase**＝Edge Function `koma-store`（合言葉）が表 `koma_docs` に丸ごと1行。コードは書いた（`supabase/functions/koma-store`・`migrations/0002_koma_docs.sql`）が**当てていない**＝どのプロジェクトに置くか（SPEC §11 ③）を決めてから。
- **SQLite**＝書き出しだけ（sql.js を書き出すときだけ CDN から読む）。列は 0001_init.sql と同じ名前。写真の中身と鍵は入れない。読み込みは JSON から。

**結果**
- 検査 65 本通過。⚠ Google ドライブと Supabase への実際の読み書きは、本人の Client ID／関数のデプロイが要るため未検証。

## v11 — 進行中の作法4つ（自動終了・常時1行・長時間の確認・🚫 は連続を切らない）（2026-09-18）

**背景**
- 似たアプリの調査（docs/reports）で「真似すべき」とした4つを、ゆう「まとめて進めてください」。

**設計判断**
- **開始で前が止まる**（Now Then）＝「開始」の合図で他の進行中をその時刻で終了。同じ種目・同じ名前が走っていれば止めない（二重開始は行が増えるだけ）。⚙ で切ると並行して走れる。
- **進行中を常時1行**（Timery）＝どのタブでも一番上に「⏵ 🏃 朝散歩 08:00〜 25分」。経過分は**保存せず時刻から計算**し1分ごとに描き直す（背面で止まる問題が構造的に起きない）。⏹ 今 終了。
- **長時間の確認**（Toggl）＝種目ごとの上限（既定 180 分・空＝聞かない）を超えると行が黄色くなり「まだ続いていますか？」＋「⏹ 上限の分で終了」。⚙ で OS の通知にも出せる（許可を取る）。
- **🚫 は第3の状態**（Way of Life・Loop）＝文言を「今日は無し」に。🔥 連続日数は 🚫 を飛ばして数える（⚙ で「切る」にもできる）。

**結果**
- 検査 63 本通過。

## v9 — 入力層＝合図（「座禅開始」「散歩終了」）（2026-09-18）

**背景**
- ゆう「入力層を考えたい。全体に対する入力と、個別種目にたいする入力。座禅開始、終了というと時刻が統一されて登録される。片方が落ちた場合にはそのまま統一されずにのこる。運動に散歩があると登録しておくと、散歩開始、散歩終了でそこに登録される」→ 提案 → 「はい」。

**設計判断**
- **合図（signal）を解く純粋な関数**（`src/domain/signal.ts`）＝動詞（開始／終了／やった／やらなかった）・時刻（8時から・8:05）・長さ（30分・1時間半）・名前。画面も置き場も知らない＝あとで Siri のショートカットからも同じ口。
- **名前の解き方**＝種目名 → 種目の別名 → ⭐ いつもの の呼び名・別名。**登録＝⭐ をそのまま使う**（新しい器を作らない）。別名は ⭐ と種目の板に欄を足した。
- **突き合わせ**＝終了は「同じ種目・同じ名前で進行中（今日か昨夜）」の直近と組む。相手が無ければ**終わりだけ**の行を作る。開始が2回なら2行。どちらも消さず ⏵／⏹ の印で見せる。
- **進行中の見分け**＝合図の印（`payload.signal='start'`）＋終わり無し。始まりの時刻だけの食事を進行中と誤らないため（負のテストで見つけた）。
- 解けない名前は **📥 未振り分け** に置き、あとで種目を選んで振る（黙って捨てない）。
- 終了で ✅ を立てる（✅ を使う種目だけ）。「散歩 30分」のように動詞が無く長さか時刻があれば「やった」扱い、何も無ければ予定として置く。
- **⏵ 進行中に話しかけると、その記録のメモに時刻つきで積もる**（ゆう追記「散歩開始と終了の間に話しかけると、メモにどんどん蓄積」）＝動詞も時刻も長さも無く、種目名としても解けない言葉が対象。進行中が無ければ今までどおり未振り分け。

**結果**
- 検査 59 本通過（合図の解き方・名前の解き方・突き合わせ・片方だけ・未振り分け・種目の欄からの省略）。

## v8 — ⏱ 合計・平均／分は5分刻み＋押しボタン／「今」／🧾 レシート（📷 → 🤖 BYOK）（2026-09-18）

**背景**
- ゆう「月、週では合計で何分、平均で何分やったかをだしたい」「1分の後が6分になる。0分から5分単位で。チップで＋30分、＋1時間、＋3時間」「時間を合わせるところに、今の時間をクリックすると今の時間が入ると便利」「レシートの種目。レシートを up すると AI が読み取って、内容が文字起こしされる。AI は Bring Your Own Key で」。

**設計判断**
- ⏱ 合計・平均＝帯に出す（🚫 は除く・長さの無い記録は数えない・平均は回あたり）。「すべて」の帯にも種目ごとの合計。保存しない＝読むときに数える。
- 分の欄＝min 0・step 5（1→6 になっていたのは min 1 のせい）。＋30分／＋1時間／＋3時間 は**足す**（押すたびに増える）。✕ で空に。
- 「今」＝時刻の欄の横。分まで入れる。実際の側は日も入れる。
- 🧾 レシート＝**種目の型**（新しい器は作らない）。写真は 📷 で選ぶ／撮る → 縮小（長辺 1568・サムネ 320＝ライフログ meals と同じ）→ 本体は IndexedDB・サムネは記録の中。鍵があれば上げた直後に読む。読み取り結果は `payload.receipt`（店・日時・合計・品目・支払い・不確かな所）。題名は空のときだけ AI が埋める（人が直したものを上書きしない）。日時が読めたら記録の日と実際の時刻に写す。
- 🤖 BYOK＝鍵は端末の設定だけ（サーバ無し）。Anthropic（ブラウザ直呼びの専用ヘッダ）と Gemini の2つ。返事は JSON だけを求め、**検査してから使う**（変な値は null）。数値は作らせない（レシートの金額は写っている数字なので可）。
- 🍽 食事にも `ai:'meal'` を付けた＝鍵があれば同じ道で読める（Phase 2 の入口が開いた）。

**結果**
- 検査 49 本通過。⚠ AI の実呼び出しは本人の鍵でしか確かめられない＝⚙ →「接続確認」で確かめる。

## v7 — ⏱ 何分・何時間 だけの入力（予定も実際も）（2026-09-18）

**背景**
- ゆう「いまは何時から何時までとしかいれれないけど、開始終了時間関係なく、何分やったとか、何時間やったとかの入力もありにしたい。予定も、実績も」。

**設計判断**
- 長さは**別の列**（`planDur`／`actualDur`・分）。始まり〜終わりから導けるものと、本人が書いた長さは意味が違う（意味が違うものを同じ列に入れない）。
- 升目に出す「何分」＝**書いた長さが勝つ**。無ければ 始まり〜終わり から計算。実際があれば実際、無ければ予定。
- 📅 の終わり＝始まり＋長さ（長さがあれば終わりの時刻より優先）。始まりが無く長さだけなら終日。
- 板では 時刻の欄の右に「⏱ [ ]分」。始まり〜終わりが入っているのに長さが空なら「（始まり〜終わりだと N分）」と添える。
- 表（SQL）に `plan_dur` `actual_dur` を足した（未適用のまま）。

## v6 — ⊞ すべて の 1日と月（2026-09-18）

**背景**
- ゆう「すべての1日と月も進めてください」（週を出した直後）。

**設計判断**
- **1日**＝種目をまたいだ**時刻順の一本の流れ**（時刻＝実際を主にする種目は実際、それ以外は予定。予定と実際がずれていれば「実際 HH:MM」を添える）。時刻の無い記録は上に**種目ごとの束**。🔁 の薄い回も時刻があれば流れに混ぜる。行の先頭の種目の絵を押すとその種目へ。
- **月**＝升に種目ごとの1行（◻️ 2/4・🍽 3・🧘 ✅）。全部 ✅ なら緑、全部 🚫 なら黄。升を押すと「すべて」の 1日 へ。
- 枡（午前／昼…）は「すべて」では出さない＝種目ごとに切り方が違うので、時刻で並べる。

## v5 — ⊞ すべて（週＝日 × 種目）（2026-09-18）

**背景**
- ゆう「全種目をトータルでみれる UI はどうすればいいんだろう」→ 提案（週＝日×種目／1日＝時刻順の一本／月＝種目アイコン）→「週の日×種目だけ先に作り、触ってから 1日と月に広げる」。

**設計判断**
- 種目タブの先頭に「⊞ すべて」。横軸を**枡ではなく種目**にする（枡は種目ごとに切り方が違い、重ねられない）。
- 升＝種目ごとの小さなまとめ（✅2/4・🚫・🔁 n・🍽 n）＋記録の名前3件まで。一日一回の種目は印そのもの（押して回せる）。升を押すとその種目のその日へ。
- 「すべて」では ＋足す／⭐／🔁 を出さない（どの種目か決まらないため）。1日・月は押せない印にして「週を触ってから」と書いた。

**結果**
- 検査 44 本通過。

## v4 — 升目に出すものをアイコンで切替（🕐 ⏱ 💬 📷）（2026-09-18）

**背景**
- ゆう「カレンダーに何時から何分やった、何分やった、コメント、写真表示したいもののオンオフがアイコンであるとわかりやすい」。

**設計判断**
- 上の帯に 🕐 何時から／⏱ 何分やった／💬 コメント／📷 写真 の4つの押しボタン。押し込み＝出す、薄い＝隠す。端末の設定（`settings.show`）として残る。既定＝時刻・分・写真は出す、コメントは隠す。
- 「何分やった」＝実際の開始〜終わり（無ければ予定）から**計算**（保存しない）。片方しか無ければ出さない。
- 写真は枚数ではなくサムネを出す（📷 を隠しているときだけ枚数の印）。写真の中身を入れる道は Phase 2。

## v3 — 月の画面・一日一回型（座禅）（2026-09-18）

**背景**
- ゆう「日、週に加えて月も出してほしい」「一日一回みたいなのにも対応したい。たとえば座禅。最低限の入力、ある日やった、やらない。詳細として時間などを使い出来る」。

**設計判断**
- 一日一回は**種目の性質**（`features.daily`）にした＝新しい器を作らない。記録は今までと同じ `Entry`（1日1行・title＝種目名）。升目だけが「印1つ」になる。
- 印は なし → ✅ → 🚫 → なし の一巡。**詳細（時刻・メモ・写真）が入っている行は「なし」でも消さない**＝入れたものを黙って捨てない。
- 🔥 連続日数＝今日が未記録なら前日から数える（朝の時点で「途切れた」に見えない）。
- 月＝月初を含む週の頭から6週（42日）で固定＝升の高さが月ごとに変わらない。

**結果**
- 検査 44 本通過。見本に 🧘 座禅（3日分）を足した（既に開いている端末は ⚙ →「見本に戻す」か「＋ 種目を足す → 一日一回型」で出る）。

## v2 — 開発のページ（GitHub Pages）・終日で出す・AI 書き起こしを段階に（2026-09-18）

**背景**
- ゆう「いつも通り開発のページで見れるようにしたい／プライベートが無理なら公開にしても大丈夫」「食事の AI 書き起こし＝いれてください」「時間帯なしを Google に終日で＝だしてください」。
- 本人が `npm run dev` を打つと **Port 5276 is already in use**＝Claude の preview_start が握っていた（memory の既知の罠）。

**設計判断**
- GitHub Pages は private では使えない（API が 422「plan does not support」）→ **public に変更**（本人の許可あり）。Actions で 検査 → 焼く → 出す（`pages.yml`）。
- 📅 時刻の無い記録は**終日**で出す（`CalendarEvent.allDay`）。説明欄に枡の名前を入れる＝「午後にやる」は伝わる・嘘の時刻は作らない。
- AI 書き起こしは Phase 2（写真と一緒）。鍵はサーバ側（Edge Function `koma-ai`）＝ライフログと同じ。数値は作らせない。

**結果**
- 検査 41 本通過。Pages の URL: https://yutsutke.github.io/dayslots/

## v1 — 全体像＝リポ・骨組み・端末で動く画面（2026-09-17）

**背景**
- ゆうの要望「ライフログのやることと食事の機能を全部入れて、発展もできれば発展させる iOS アプリ。専用のリポから。午前／昼／午後／夕以降・朝食／昼食／夜食／間食 に分けて記録。予定時刻・実行時刻が決まっていれば見える。境目の時間は変えられ、区切りは増減できる。朝散歩・夜ジムのような運動にも広げたい。⭐ いつもの・🔁 繰り返しの作法を真似る。Google カレンダーに詳細な時間を出せる」。
- ライフログ（`owntracks-supabase-notion`）の `todos`／`todo_recurring`／`meals`／`meal_presets`／`meal_recurring`／`recur.js`／`gcal` を読み、規則を棚卸しした（SPEC.md §2）。

**設計判断**
- **種目 × 枡** に一般化＝やること・食事・運動が同じ器。枡は「時刻で決まる枡（始まりを持つ）」と「時刻で決まらない枡（時間帯なし・間食）」の2種類。隙間は受け皿へ（食事の 15:00〜16:30）。
- **境目は保存せず読むときに導く**（ライフログの判断をそのまま）。人が選んだ枡が勝つ。消えた枡の値は書き換えない。
- ✅🚫🔀・⭐・🔁 の約束はライフログの Edge Function から**写した**（両立しない・導出は保存しない・未来に作らない・(rule, date) で1行・型や規則を消しても記録は残る）。
- **Capacitor ＋ Vite ＋ TypeScript（枠組み無し）**＝既に動いている作法（あの日・声で入れるカレンダー）に揃える。Expo は「新しい道具が丸ごと増える」ので採らず。
- データは **v1＝端末**、Supabase・Google は**口だけ**先に決めた（`Store`／`CalendarPort`）。表の形は `supabase/migrations/0001_init.sql`（未適用）。
- Google に出すのは**時刻がある記録だけ**（枡だけの記録に嘘の時刻を作らない）。

**ハマったところ**
- Bash の heredoc で日本語を含む TS を書くと `'` の対応が壊れることがあった → ファイルは Write ツールで書いた（原因は追っていない）。

**結果**
- `npm test`（枡の決まり・🔁 の展開・台帳の規則・📅 の判定）と `npm run build`（型検査つき）が通る。`npm run dev` で週／1日の画面・記録／⭐／🔁／⚙ の板が動く（端末の中だけ）。

**教訓**
- ライフログの規則は**表の CHECK と Edge Function のコメント**に全部書いてある＝新しい器に写すときはそこを読めば足りる（画面の 31k 行を読む必要はなかった）。
