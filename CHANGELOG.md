# CHANGELOG

## v21 — ズレを読む（予定と実際の差）（2026-09-20）

**背景**
- ゆうとの相談「わかりやすい説明とはなんだろう」から、アプリを **入力層／記録層／読み解く層（読むときに決める）／出力層** の4つで切り直した。「時間」の枝だけ性質が違う＝いくら足しても記録の列が1つも増えない（v17〜v20 がまさにそれ）＝それは読み解く層だから、と分かった。
- その見方で各層の「次の一手」を並べたとき、読み解く層に穴が見えた。**`planStart` と `actualStart` を別の列として持つと最初に決めた（憲法2条）のに、その差をどこでも計算していない**。コードで確かめたら本当に1か所も無く、`app.ts` で実際の時刻を併記しているだけだった。
- 調査（docs/reports）でも「予定 vs 実績と『平均』は薄い」＝似たアプリ約60本を見ても空白。
- 手が軽い（記録に1列も足さない）のに効きが大きい（📝 振り返りにそのまま乗る）ので、ここに集中すると決めた。

**設計判断**
- 規則は `src/domain/gap.ts` ただ1つ（憲法5条）。**差は保存せず、読むときに引く**（憲法1条）＝予定を後から直せば過去のズレもその場で読み直る（検査あり）。
- **3つのズレを混ぜない**（憲法2条）＝時刻／長さ／日。予定 8:00 の用を2日後の 8:30 にやったら「時刻 30分おそい」かつ「2日おくれ」。足して1つの数にしない。
- **片方しか無ければ `null`＝「分からない」**。0（ちょうど）とは別物で、まとめの件数にも入れない（憲法7条＝分からない数字を作らない）。「予定だけ置いた記録」を 0 として平均に混ぜると、平均が嘘になる。
- **🚫 やらなかった 記録は比べない**＝実際の時刻が残っていても見ない。やらなかったことに「遅れ」も「早い」も無い。
- 長さのズレは `planDurationOf` / `actualDurationOf` を**そのまま使う**＝「書いた『何分』が勝つ」決まりを二重に書かない。
- **符号（＋ー）を使わず日本語で書く**（「11分おそい」「15分ながい」「2日おくれ」）＝向きを覚える手間を人に渡さない。言い方も `gap.ts` の1か所。
- まとめは 平均・件数・遅れ／早いの数・いちばん大きいズレ。**1件のときは「最大」を書かない**（同じ数を2回言うと読みにくい）。
- 出る所＝1日の行（予定・実際の下に控えめに）／見直しの帯／📝 振り返り（1件ごと＋7日のまとめ）。

**ハマった所**
- 「最大」を**符号つきで絶対値が最大のもの**と定義したら、自分で書いた検査の期待値のほうが間違っていた（+11／+5／−10 で −10 を期待していたが、正解は +11）。コードが正しく、テストを直した。
- 1件しか無いときに「平均 ちょうど（1件・最大 ちょうど）」と出て読めなかった → 件数1のときは「最大」を落とす規則を足した。
- 画面の行に「予定より5分おそい」を出したら、時刻の列（120px）で「おそ／い」と割れた → **行では「予定より」を省き（予定と実際が真上に並んでいるので迷わない）、📝 振り返りでは残す**（AI には列の助けが無い）。`gapLine(e, withPrefix)` の1引数で分けた。

**結果**
- 検査 122 本通過（v21 で 14 本追加・うち負のテスト5本）。`npm run build` 通過。
- 画面で確認＝運動の1日で「予定 06:30–07:00 ／ 実際 06:35–07:05 ／ 5分おそい」、帯に「始まりのズレ 5分おそい（1件）・長さのズレ ちょうど（1件）」。
- 📝 振り返りに「✅ 朝散歩 06:35〜07:05 ⏱30分 予定より5分おそい」「予定とのズレ＝始まり 5分おそい（1件）／長さ ちょうど（1件）」が入った。

**教訓**
- **別々の列に分けて持ったものは、いつか引き算して見せないと、分けた意味が本人に届かない。** 予定と実際を分けたのは v1 だったのに、差を出すまで20版かかった。分けた時点で「この2つをどう並べて見せるか」も宿題として書いておくべきだった。
- 層で切り直すと穴が見える。機能の一覧では「予定も実際も持っている」で終わってしまい、「持っているが使っていない」は見えない。
- 先送りの半分はもう記録されていた＝`date` と `actualDate` が別の列なので、済んだ後のズレは読めばいい。新しい列が要るのは「まだやっていないまま日を動かす」ほうだけ、と範囲が狭まった。

## v20 — 升目の列も「1日の始まり」の枡から並べる（2026-09-20）

**背景**
- ゆう（v19 を見て）「日の入りを選んだら、時間帯なし、夜、夜、夜明け前、…、午後になるのが正解では？」＝そのとおり。v19 は「どの日に見せるか」だけ変えて、列は 0:00 始まりの並びのままだった。

**設計判断**
- 列の並び＝**1日の始まりの時刻を含む枡から1周**（`displaySlots`・`src/domain/slots.ts`）。日の入り始まり＝夜 → 深夜 → 朝 → 昼 → 午後／日の出始まり＝朝 → … → 深夜。0:00 始まりは ⚙ で決めた並びそのまま。
- 時刻で決まらない枡（時間帯なし・間食）は ⚙ の位置のまま動かさない（先頭の「時間帯なし」は先頭、最後の「間食」は最後）。
- 効く所＝週の升目と 1日の一覧。記録の板の枡のボタンは ⚙ の並びのまま（選ぶ場所が日で動くと押し間違える）。

**結果**
- 検査 108 本通過。画面で確認＝日の入り始まりで「時間帯なし／夜 ≈17:45〜23:35／深夜 ≈23:35〜翌04:55／朝／昼／午後 ≈13:35〜17:45」。

**教訓**
- 「日の区切り」を変える機能は、①どの日に入るか ②その日の中の並び の2つが組。片方だけ直すと、見た目が半分だけ古いままになる。

## v19 — 1日の始まりを 0:00／日の出／日の入り から選ぶ（2026-09-20）

**背景**
- ゆう（v18 の感想）「基準日はいいね。何日たっているかの情報は感情をうごかす」「途中やあとで なに を決められるのもいいし、とりあえず始められるのもいい」「夜を等分＝これで江戸時代の時計を再現できるようになりました」→「一日の始まりを日の出や日の入りにするビューがほしいね」。

**設計判断**
- ⚙ →「☀ 日の出・日の入りの場所」に **1日の始まり**＝🕛 0:00／🌅 日の出／🌇 日の入り（±分のずらし）。
- **どの日に見せるか、だけを読むときに決める**（`src/domain/viewday.ts`・`repo.viewDate(e)`）。記録の暦の日付と時刻は書き換えない＝枡の境目と同じ約束。0:00 に戻せば元どおり。
  - 🌅 日の出始まり＝その日の日の出より前の時刻は前の日に見せる（未明の記録は前の日の続き＝江戸の明け六つ始まり）。
  - 🌇 日の入り始まり＝その日の日の入り以後は次の日に見せる。
  - 時刻の無い記録は暦の日のまま。
- 効く所＝週・1日・月・リスト・すべて・見直しの帯・🔥 連続・⏱ 合計（`entriesFor` を見せる日で切る1か所に集めた）。「今日」も見せる日（未明はまだ前の日）。週の日付の列に「🌅05:25〜」とその日の始まりを出す。「すべて」の1日は、その日の始まりからの順に並ぶ（23:00 の後に翌 2:00）。
- 記録の板＝暦の日と見せる日が違うときだけ「☀ 見せる日」の一言を出す。
- 🔁 の回は今までどおり暦の日（ruleDate）で1行＝二重に作られない（検査あり）。

**結果**
- 検査 107 本通過。

## v18 — ▶ スタート／⏹ ストップ・なに未定の計測／リストの基準日／🌙 夜を等分（2026-09-20）

**背景**
- ゆう「リストは基準日を決められて、そこから何日経過を出せるように」「昼の分割があるなら、夜を分割もほしい」「今の時間からスタートボタン、ストップボタンがほしい。種目を選択している場合＝候補が1つならそれで記録・複数なら選択してからスタート。すべてを選択している場合＝スタート、ストップは押せる、計っている最中でも計り終わってからでも なに を決められるように」。

**設計判断**
- **▶／⏹**＝合図の欄の左。種目タブでは候補＝その種目の ⭐ いつもの（無ければ種目の名前＝座禅など）。1つならそのまま開始、複数なら選ぶ板（＋名前を入れる）。合図の「開始」と同じ結果だが、**選んだ名前を解釈し直さない**（「30分ジョグ」が長さに化けない）。
- **なに未定の計測（Timer）**＝「⊞ すべて」の ▶。種目が決まっていないので記録（Entry）にはできない＝別の小さな器（`db.timers`）。最中に決めれば ⏵ 進行中の記録に、終わってから決めれば 始まり〜終わり の入った記録（✅ を使う種目は ✅）になり、器からは消える。決めるまではどのタブにも「⏱ なに未定の計測」の箱が出る（忘れない）。計測中に話しかけた言葉はメモに積み、決めたとき記録に引き継ぐ。
- 「次を始めると前が止まる」は計測にも同じ規則（`stopOthers` の1か所にまとめた）。
- **リストの基準日**＝日付の横に「基準日から N日／N日前」。端末の設定に残る（空＝今日）。
- **🌙 夜を等分**＝日の入り → 次の日の出 を N 等分。夜は 0:00 をまたぐので、境目が 24:00 より前ならその日の夜、後なら未明の側に出る。**その日いちばん遅く始まる枡が ☀ なら、未明（0:00〜最初の枡）はその枡の続き**にした＝受け皿に落ちない（見出しは「≈23:35〜翌04:55」）。☀ を使わない種目（食事の未明＝間食）は今までどおり。
- 「☀ 太陽で切る枡にする」は 朝／昼／午後／夜／深夜（夜を2等分）に変えた（夜明け前 0:00 の固定枡をやめ、深夜が朝まで続く）。

**結果**
- 検査 100 本通過。画面で確認＝すべての ▶ → ⏹ →「なにを決める」→ 座禅として記録／運動の ▶ は候補2つから選ぶ → 座禅が自動終了／リストの基準日 9/10 →「基準日から 10日」。

## v17 — 📋 リスト／☀ 日の出・日の入りで切る枡（2026-09-20）

**背景**
- ゆう「一日、週、月にくわえて、リストビューもほしい」「コマの分け目を日の出や日の入りでもわけれるようにしたい。精度は5分単位ぐらいで十分。日の出の何分前や、日の出から日の入りを3等分するとかにも対応してほしい」。

**設計判断**
- **📋 リスト**＝日をまたいで縦に並べる。◻️ まだ（古い順＝先に片づける順・過ぎた日は色を変える）／✅ やった／🚫 今日は無し／ぜんぶ（新しい順）。種目タブでも「⊞ すべて」でも同じ形（すべて では種目の絵を付ける）。範囲は直近90日〜いちばん先の記録（「もっと前も出す」で全部）。日付を押すとその日の 1日 へ。リストは見るだけなので 🔁 の自動の回は作らない。
- **☀ の枡**＝枡の始まりに「🕐 時刻／🌅 日の出±分／🌇 日の入り±分／☀ 昼を N 等分した何つ目」を選べる（`SlotDef.sun`）。計算は端末の中（NOAA の簡易式・誤差1〜2分・**5分刻みに丸め**）・通信なし。場所は ⚙ →「☀ 日の出・日の入りの場所」（緯度経度／📍 現在地／既定は東京）。
- **その記録の日の太陽で決める**＝同じ 5:10 でも夏は「朝」・冬は「夜明け前」。境目は今までどおり保存せず読むときに導く＝記録は1文字も書き換えない。見出しの範囲には「≈」を付ける（日で変わるため）。
- 白夜・極夜で決まらない日は、枡の素の時刻（控え）に戻る。☀ の枡は季節で順が入れ替わることがあるので、並びはその日の始まりで並べ直す。検査の「同じ時刻」「食い込み」は ☀ でない枡どうしだけ。
- 枡の編集に「☀ 太陽で切る枡にする」＝夜明け前／朝（日の出の30分前）／昼・午後（昼を3等分）／夜（日の入り）を一度に作る。

**ハマったところ**
- リストの範囲の端を `9999-12-31` にしたら、帯が 🔁 の回をその日まで1日ずつ数えに行って**画面が固まった**（プレビューの確認道具ごと固まり、最初は道具側の不調に見えた）。→ 範囲は実データの端まで。さらに `occurrences()` に「窓は3年まで」の最後の砦と負のテストを足した。

**結果**
- 検査 90 本通過。今日（東京）＝日の出 05:25・日の入り 17:45 → 枡は 04:55／09:30／13:40／17:45。

**教訓**
- 「ぜんぶ」を表すのに遠い日付を端に使わない。日を1日ずつ回す処理がどこかに必ずある。

## v16 - 振り返りの要約を一緒に置く（AI はこれだけ読めば足りる）（2026-09-18）

**背景**
- ゆう「AI が Supabase の情報を読む負担を下げるには、どう保存するのがいい？ 日々の振り返りだと1週間分あれば十分」→ 要約を別に置く案 →「要約を渡すのはいいですね。進めてください」。

**設計判断**
- 送るたびに、アプリが**直近7日の読み物**（日ごと・種目ごと・時刻と長さと印・代わりに・メモの先頭60文字・7日の合計）を作り、koma_docs の列 **review**（jsonb・text が本体）に置く。Google ドライブなら koma-review.md。
- **計算はアプリの決まりで済ませてから渡す**（枡・何分・連続日数）＝AI や SQL に同じ規則をもう一度書かせない。id・内部名・作成時刻は出さない。
- これは写しの一部（読み物）で、元の記録には何も足さない＝「導出値は保存しない」の約束のまま。分析の正は doc。
- 要約が作れなくても記録は送る。要約が付いていない PUT は前の要約を残す。
- 設定に「7日／14日／31日ぶんを見る」と コピー を足した＝Supabase を通さず、その場で AI に貼ることもできる。

**結果**
- 検査 81 本通過。表に列 review を足し（migration 0003・本番に適用済み）、関数を置き直した。

## v15 - 別の端末でつなぐときの穴を塞ぐ（初回は聞く・戻ってきたら確かめる）（2026-09-18）

**背景**
- ゆう「別の端末で操作すると画面が違う。どう解決すべき？」＝端末ごとに保存箱が別で、保存場所の設定も端末ごと（わざと外に送っていない）。
- 調べて見つけた穴＝「新しい方が勝つ」だけだと、新しい端末は開いた瞬間に見本を保存して時刻が一番新しくなる → つないだ途端に**見本で外の写しを上書き**する。

**設計判断**
- **一度も合わせていない端末では自動で決めない**（remoteSavedAt が無い間）。外に写しがあれば、自動（開いたとき・保存の3秒後）は何もせず「今 合わせる で選んで」と伝える。「今 合わせる」で **外を取り込む／この端末を送る／やめる** を聞く。外に何も無ければ聞かずに送る。
- 初回の取り込みは**丸ごと置き換え**（この端末の中身は見本か試し書き）。鍵と保存場所の設定だけ端末のものを残す。
- **画面に戻ってきたら外を確かめる**（visibilitychange）。since つきなので、外が新しくなければ中身は落ちてこない。

**結果**
- 検査 78 本通過（負のテスト＝時刻が新しい見本の端末をつないでも、外の写しが書き換わらない）。

## v14 - 差分で送る・新しいときだけ落とす（Supabase）（2026-09-18）

**背景**
- ゆう「1か月の前に、前回送ったときからの差分というのもあったほうがよいね」。

**設計判断**
- **送る**＝前回送れた時刻より後に変わった記録（upserts）と、消した記録の id（deletes）だけ。種目・いつもの・繰り返し・設定（芯）は小さいので毎回丸ごと。関数 koma-store が持っている写しに当てる。期間つきなら当てたあと期間より前を落とす＝外の写しが太らない。
- **土台の確認**＝端末が知っている「外の savedAt」と外が食い違えば 409 → 端末は丸ごと送り直す（別の端末が先に書いた・写しが無い・取り込み直後など）。
- **落とす**＝GET に since を付け、外が新しくなければ中身を返さない。Supabase の転送量は「落とす方」が数えられるので、こちらの方が効く（今までは開くたびに丸ごと落としていた）。
- **規則は1か所**＝差分を作る／当てるは src/sync/delta.ts だけ。関数の側はその写しで、検査が「1文字も違わない」ことを見る。
- 消した記録の id は db.deleted に控え、送れたら空にする。JSON 読み込み・見本に戻す のあとは丸ごと送り直す。
- Google ドライブは関数が無いので今までどおり丸ごと（期間で切るのは効く）。

**結果**
- 検査 73 本通過（差分を当てた結果＝丸ごと送った結果、を含む）。関数は version 3 として置き直した。

## v13 - 送る期間を選べる（直近1か月／3か月／1年／すべて）（2026-09-18）

**背景**
- ゆう「変更のたびに全部送るのは重くない？」→「毎回 UP するのを直近1か月などに限定できるようにする。まとめて分析したいときだけ期間を長くすれば Supabase でいけるのでは」。

**設計判断**
- 外に送る記録を期間で切る（種目・いつもの・繰り返し・設定はいつも全部＝小さい）。写しに window.from（この日から先のぶんだけ入っている）を書く。
- **取り込みは期間の中だけ置き換える**＝期間より前の記録は端末のものを残す。これが無いと、1か月ぶんの写しを取り込んだ瞬間に古い記録が端末から消える（負のテストあり）。
- 期間の中の削除・追加はそのまま伝わる。期間なし（すべて）の写しは今までどおり丸ごと置き換え。
- 引き換えに失うもの＝期間より前の記録は外に無い（別の端末からは見えない・端末を失うと消える）＝控えは JSON 書き出しか Drive に。分析のときだけ「1年／すべて」にして送り、戻す。

**結果**
- 検査 70 本通過。

## v12.1 — Supabase の写しが実際に動いた（2026-09-18）

- ゆうが本番プロジェクト（ライフログと同じ）に表 `koma_docs`・Secret・関数 `koma-store` を置いて試した → 「Failed to fetch」。原因＝関数が OPTIONS（ブラウザの事前確認）に**本文つきの 204** を返して 500 になっていた（Deno は 204 に本文を許さない）。本文なしに直して置き直し → 「端末の内容を外へ保存しました」（56 件・36KB）。
- **外の写しに 🤖 の鍵と保存場所の設定を入れない**ようにした（取り込むときは端末のものを戻す）＝外に秘密を置かない。
- SPEC §11 ③（同じプロジェクトか別か）＝**試しは同じプロジェクト**で進めることになった（表名 `koma_` でぶつからない）。

## v12 — 種目の並べ替え／保存場所（端末・Google ドライブ・Supabase）／SQLite 書き出し（2026-09-18）

**背景**
- ゆう「トップページで並び替え。設定でも並び替え」「保存場所＝Google ドキュメントを指定して保存（JSON と SQLite）。Supabase も指定できるように」。

**設計判断**
- **並べ替え**＝⚙ の一覧に ↑↓、トップは選んでいる種目のタブの横に ‹ ›（動かすのは選んでいるものだけ＝誤タップで並びが崩れない）。畳んだ種目は ⚙ からだけ 🗑 で消せる（記録の数を言ってから）。
- **保存場所＝「外の写し」**＝端末の保存箱はいつも使い、その丸ごと1つの JSON を外に置く。開いたときに外が新しければ取り込み、保存のたびに3秒後に押し出す。**新しい方（savedAt）が勝つ**。行単位の同期は Phase 3 の宿題（1人1台ずつの前提）。
- **Google ドライブ**＝Google Identity Services（ブラウザで Google のログイン窓を開く方式）・権限 drive.file（このアプリが作ったファイルだけ）・本人が指定した**フォルダ**に koma.json。「Google ドキュメント」は文書の形式なので JSON/SQLite を入れられず、フォルダ指定にした。Client ID は本人が Google Cloud で作る（手順は ⚙ の説明）。⚠ iOS の殻の中ではこのログイン窓が開かないことがある＝TODO。
- **Supabase**＝Edge Function `koma-store`（合言葉）が表 `koma_docs` に丸ごと1行。コードは書いた（`supabase/functions/koma-store`・`migrations/0002_koma_docs.sql`）が**当てていない**＝どのプロジェクトに置くか（SPEC §11 ③）を決めてから。
- **SQLite**＝書き出しだけ（sql.js を書き出すときだけ CDN から読む）。列は 0001_init.sql と同じ名前。写真の中身と鍵は入れない。読み込みは JSON から。

**結果**
- 検査 65 本通過。⚠ Google ドライブと Supabase への実際の読み書きは、本人の Client ID／関数のデプロイが要るため未検証。

## v11 — 進行中の作法4つ（自動終了・常時1行・長時間の確認・🚫 は連続を切らない）（2026-09-18）

**背景**
- 似たアプリの調査（docs/reports）で「真似すべき」とした4つを、ゆう「まとめて進めてください」。

**設計判断**
- **開始で前が止まる**（Now Then）＝「開始」の合図で他の進行中をその時刻で終了。同じ種目・同じ名前が走っていれば止めない（二重開始は行が増えるだけ）。⚙ で切ると並行して走れる。
- **進行中を常時1行**（Timery）＝どのタブでも一番上に「⏵ 🏃 朝散歩 08:00〜 25分」。経過分は**保存せず時刻から計算**し1分ごとに描き直す（背面で止まる問題が構造的に起きない）。⏹ 今 終了。
- **長時間の確認**（Toggl）＝種目ごとの上限（既定 180 分・空＝聞かない）を超えると行が黄色くなり「まだ続いていますか？」＋「⏹ 上限の分で終了」。⚙ で OS の通知にも出せる（許可を取る）。
- **🚫 は第3の状態**（Way of Life・Loop）＝文言を「今日は無し」に。🔥 連続日数は 🚫 を飛ばして数える（⚙ で「切る」にもできる）。

**結果**
- 検査 63 本通過。

## v9 — 入力層＝合図（「座禅開始」「散歩終了」）（2026-09-18）

**背景**
- ゆう「入力層を考えたい。全体に対する入力と、個別種目にたいする入力。座禅開始、終了というと時刻が統一されて登録される。片方が落ちた場合にはそのまま統一されずにのこる。運動に散歩があると登録しておくと、散歩開始、散歩終了でそこに登録される」→ 提案 → 「はい」。

**設計判断**
- **合図（signal）を解く純粋な関数**（`src/domain/signal.ts`）＝動詞（開始／終了／やった／やらなかった）・時刻（8時から・8:05）・長さ（30分・1時間半）・名前。画面も置き場も知らない＝あとで Siri のショートカットからも同じ口。
- **名前の解き方**＝種目名 → 種目の別名 → ⭐ いつもの の呼び名・別名。**登録＝⭐ をそのまま使う**（新しい器を作らない）。別名は ⭐ と種目の板に欄を足した。
- **突き合わせ**＝終了は「同じ種目・同じ名前で進行中（今日か昨夜）」の直近と組む。相手が無ければ**終わりだけ**の行を作る。開始が2回なら2行。どちらも消さず ⏵／⏹ の印で見せる。
- **進行中の見分け**＝合図の印（`payload.signal='start'`）＋終わり無し。始まりの時刻だけの食事を進行中と誤らないため（負のテストで見つけた）。
- 解けない名前は **📥 未振り分け** に置き、あとで種目を選んで振る（黙って捨てない）。
- 終了で ✅ を立てる（✅ を使う種目だけ）。「散歩 30分」のように動詞が無く長さか時刻があれば「やった」扱い、何も無ければ予定として置く。
- **⏵ 進行中に話しかけると、その記録のメモに時刻つきで積もる**（ゆう追記「散歩開始と終了の間に話しかけると、メモにどんどん蓄積」）＝動詞も時刻も長さも無く、種目名としても解けない言葉が対象。進行中が無ければ今までどおり未振り分け。

**結果**
- 検査 59 本通過（合図の解き方・名前の解き方・突き合わせ・片方だけ・未振り分け・種目の欄からの省略）。

## v8 — ⏱ 合計・平均／分は5分刻み＋押しボタン／「今」／🧾 レシート（📷 → 🤖 BYOK）（2026-09-18）

**背景**
- ゆう「月、週では合計で何分、平均で何分やったかをだしたい」「1分の後が6分になる。0分から5分単位で。チップで＋30分、＋1時間、＋3時間」「時間を合わせるところに、今の時間をクリックすると今の時間が入ると便利」「レシートの種目。レシートを up すると AI が読み取って、内容が文字起こしされる。AI は Bring Your Own Key で」。

**設計判断**
- ⏱ 合計・平均＝帯に出す（🚫 は除く・長さの無い記録は数えない・平均は回あたり）。「すべて」の帯にも種目ごとの合計。保存しない＝読むときに数える。
- 分の欄＝min 0・step 5（1→6 になっていたのは min 1 のせい）。＋30分／＋1時間／＋3時間 は**足す**（押すたびに増える）。✕ で空に。
- 「今」＝時刻の欄の横。分まで入れる。実際の側は日も入れる。
- 🧾 レシート＝**種目の型**（新しい器は作らない）。写真は 📷 で選ぶ／撮る → 縮小（長辺 1568・サムネ 320＝ライフログ meals と同じ）→ 本体は IndexedDB・サムネは記録の中。鍵があれば上げた直後に読む。読み取り結果は `payload.receipt`（店・日時・合計・品目・支払い・不確かな所）。題名は空のときだけ AI が埋める（人が直したものを上書きしない）。日時が読めたら記録の日と実際の時刻に写す。
- 🤖 BYOK＝鍵は端末の設定だけ（サーバ無し）。Anthropic（ブラウザ直呼びの専用ヘッダ）と Gemini の2つ。返事は JSON だけを求め、**検査してから使う**（変な値は null）。数値は作らせない（レシートの金額は写っている数字なので可）。
- 🍽 食事にも `ai:'meal'` を付けた＝鍵があれば同じ道で読める（Phase 2 の入口が開いた）。

**結果**
- 検査 49 本通過。⚠ AI の実呼び出しは本人の鍵でしか確かめられない＝⚙ →「接続確認」で確かめる。

## v7 — ⏱ 何分・何時間 だけの入力（予定も実際も）（2026-09-18）

**背景**
- ゆう「いまは何時から何時までとしかいれれないけど、開始終了時間関係なく、何分やったとか、何時間やったとかの入力もありにしたい。予定も、実績も」。

**設計判断**
- 長さは**別の列**（`planDur`／`actualDur`・分）。始まり〜終わりから導けるものと、本人が書いた長さは意味が違う（意味が違うものを同じ列に入れない）。
- 升目に出す「何分」＝**書いた長さが勝つ**。無ければ 始まり〜終わり から計算。実際があれば実際、無ければ予定。
- 📅 の終わり＝始まり＋長さ（長さがあれば終わりの時刻より優先）。始まりが無く長さだけなら終日。
- 板では 時刻の欄の右に「⏱ [ ]分」。始まり〜終わりが入っているのに長さが空なら「（始まり〜終わりだと N分）」と添える。
- 表（SQL）に `plan_dur` `actual_dur` を足した（未適用のまま）。

## v6 — ⊞ すべて の 1日と月（2026-09-18）

**背景**
- ゆう「すべての1日と月も進めてください」（週を出した直後）。

**設計判断**
- **1日**＝種目をまたいだ**時刻順の一本の流れ**（時刻＝実際を主にする種目は実際、それ以外は予定。予定と実際がずれていれば「実際 HH:MM」を添える）。時刻の無い記録は上に**種目ごとの束**。🔁 の薄い回も時刻があれば流れに混ぜる。行の先頭の種目の絵を押すとその種目へ。
- **月**＝升に種目ごとの1行（◻️ 2/4・🍽 3・🧘 ✅）。全部 ✅ なら緑、全部 🚫 なら黄。升を押すと「すべて」の 1日 へ。
- 枡（午前／昼…）は「すべて」では出さない＝種目ごとに切り方が違うので、時刻で並べる。

## v5 — ⊞ すべて（週＝日 × 種目）（2026-09-18）

**背景**
- ゆう「全種目をトータルでみれる UI はどうすればいいんだろう」→ 提案（週＝日×種目／1日＝時刻順の一本／月＝種目アイコン）→「週の日×種目だけ先に作り、触ってから 1日と月に広げる」。

**設計判断**
- 種目タブの先頭に「⊞ すべて」。横軸を**枡ではなく種目**にする（枡は種目ごとに切り方が違い、重ねられない）。
- 升＝種目ごとの小さなまとめ（✅2/4・🚫・🔁 n・🍽 n）＋記録の名前3件まで。一日一回の種目は印そのもの（押して回せる）。升を押すとその種目のその日へ。
- 「すべて」では ＋足す／⭐／🔁 を出さない（どの種目か決まらないため）。1日・月は押せない印にして「週を触ってから」と書いた。

**結果**
- 検査 44 本通過。

## v4 — 升目に出すものをアイコンで切替（🕐 ⏱ 💬 📷）（2026-09-18）

**背景**
- ゆう「カレンダーに何時から何分やった、何分やった、コメント、写真表示したいもののオンオフがアイコンであるとわかりやすい」。

**設計判断**
- 上の帯に 🕐 何時から／⏱ 何分やった／💬 コメント／📷 写真 の4つの押しボタン。押し込み＝出す、薄い＝隠す。端末の設定（`settings.show`）として残る。既定＝時刻・分・写真は出す、コメントは隠す。
- 「何分やった」＝実際の開始〜終わり（無ければ予定）から**計算**（保存しない）。片方しか無ければ出さない。
- 写真は枚数ではなくサムネを出す（📷 を隠しているときだけ枚数の印）。写真の中身を入れる道は Phase 2。

## v3 — 月の画面・一日一回型（座禅）（2026-09-18）

**背景**
- ゆう「日、週に加えて月も出してほしい」「一日一回みたいなのにも対応したい。たとえば座禅。最低限の入力、ある日やった、やらない。詳細として時間などを使い出来る」。

**設計判断**
- 一日一回は**種目の性質**（`features.daily`）にした＝新しい器を作らない。記録は今までと同じ `Entry`（1日1行・title＝種目名）。升目だけが「印1つ」になる。
- 印は なし → ✅ → 🚫 → なし の一巡。**詳細（時刻・メモ・写真）が入っている行は「なし」でも消さない**＝入れたものを黙って捨てない。
- 🔥 連続日数＝今日が未記録なら前日から数える（朝の時点で「途切れた」に見えない）。
- 月＝月初を含む週の頭から6週（42日）で固定＝升の高さが月ごとに変わらない。

**結果**
- 検査 44 本通過。見本に 🧘 座禅（3日分）を足した（既に開いている端末は ⚙ →「見本に戻す」か「＋ 種目を足す → 一日一回型」で出る）。

## v2 — 開発のページ（GitHub Pages）・終日で出す・AI 書き起こしを段階に（2026-09-18）

**背景**
- ゆう「いつも通り開発のページで見れるようにしたい／プライベートが無理なら公開にしても大丈夫」「食事の AI 書き起こし＝いれてください」「時間帯なしを Google に終日で＝だしてください」。
- 本人が `npm run dev` を打つと **Port 5276 is already in use**＝Claude の preview_start が握っていた（memory の既知の罠）。

**設計判断**
- GitHub Pages は private では使えない（API が 422「plan does not support」）→ **public に変更**（本人の許可あり）。Actions で 検査 → 焼く → 出す（`pages.yml`）。
- 📅 時刻の無い記録は**終日**で出す（`CalendarEvent.allDay`）。説明欄に枡の名前を入れる＝「午後にやる」は伝わる・嘘の時刻は作らない。
- AI 書き起こしは Phase 2（写真と一緒）。鍵はサーバ側（Edge Function `koma-ai`）＝ライフログと同じ。数値は作らせない。

**結果**
- 検査 41 本通過。Pages の URL: https://yutsutke.github.io/dayslots/

## v1 — 全体像＝リポ・骨組み・端末で動く画面（2026-09-17）

**背景**
- ゆうの要望「ライフログのやることと食事の機能を全部入れて、発展もできれば発展させる iOS アプリ。専用のリポから。午前／昼／午後／夕以降・朝食／昼食／夜食／間食 に分けて記録。予定時刻・実行時刻が決まっていれば見える。境目の時間は変えられ、区切りは増減できる。朝散歩・夜ジムのような運動にも広げたい。⭐ いつもの・🔁 繰り返しの作法を真似る。Google カレンダーに詳細な時間を出せる」。
- ライフログ（`owntracks-supabase-notion`）の `todos`／`todo_recurring`／`meals`／`meal_presets`／`meal_recurring`／`recur.js`／`gcal` を読み、規則を棚卸しした（SPEC.md §2）。

**設計判断**
- **種目 × 枡** に一般化＝やること・食事・運動が同じ器。枡は「時刻で決まる枡（始まりを持つ）」と「時刻で決まらない枡（時間帯なし・間食）」の2種類。隙間は受け皿へ（食事の 15:00〜16:30）。
- **境目は保存せず読むときに導く**（ライフログの判断をそのまま）。人が選んだ枡が勝つ。消えた枡の値は書き換えない。
- ✅🚫🔀・⭐・🔁 の約束はライフログの Edge Function から**写した**（両立しない・導出は保存しない・未来に作らない・(rule, date) で1行・型や規則を消しても記録は残る）。
- **Capacitor ＋ Vite ＋ TypeScript（枠組み無し）**＝既に動いている作法（あの日・声で入れるカレンダー）に揃える。Expo は「新しい道具が丸ごと増える」ので採らず。
- データは **v1＝端末**、Supabase・Google は**口だけ**先に決めた（`Store`／`CalendarPort`）。表の形は `supabase/migrations/0001_init.sql`（未適用）。
- Google に出すのは**時刻がある記録だけ**（枡だけの記録に嘘の時刻を作らない）。

**ハマったところ**
- Bash の heredoc で日本語を含む TS を書くと `'` の対応が壊れることがあった → ファイルは Write ツールで書いた（原因は追っていない）。

**結果**
- `npm test`（枡の決まり・🔁 の展開・台帳の規則・📅 の判定）と `npm run build`（型検査つき）が通る。`npm run dev` で週／1日の画面・記録／⭐／🔁／⚙ の板が動く（端末の中だけ）。

**教訓**
- ライフログの規則は**表の CHECK と Edge Function のコメント**に全部書いてある＝新しい器に写すときはそこを読めば足りる（画面の 31k 行を読む必要はなかった）。
